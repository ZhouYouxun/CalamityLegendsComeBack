// AzureThunderGroundSword.cs 的说明文件。
// 地剑由右键、自动召剑或其他联动生成，是青霆剑被动和右键系统的核心数量资源。
// 默认模式下地剑插地待机，部分饰品会让前三把地剑绕玩家椭圆环绕。
// BeginDive 会把地剑切换为俯冲模式，重置计时并写入目标点、伤害和击退。
// 俯冲前 12 帧只蓄势不伤害，之后才打开 friendly 并向目标高速下落。
// 生成时会先尝试向下对齐地面，并生成一条纯视觉锻造雷表现“天雷铸剑”。
// PulseLightningOutline 用于右键雷击序列，让范围内地剑同步闪描边。
