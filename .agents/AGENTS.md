# Custom Rules

- 除非明确点名要删除某个特效、弹幕、释放分支、声音、命中特效或机制，否则不能擅自删除、禁用、停止生成、绕过或替换它。
- 当用户说“优化”“增强”“改手感”“改视觉”“调整”时，默认都必须理解为在现有基础上继续加、调数值、调颜色、调频率、调速度、调大小，而不是删掉已有东西。
- 尤其是一个武器/技能会释放多种弹幕时，如果仅要求修改其中一种，必须保留其它所有弹幕。即使它们写在同一个函数里，也应该先拆开函数，修改被点名的部分，保留未点名的部分。
